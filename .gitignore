# Ignorer le dossier de l'environnement virtuel
venv/
.venv/

# Ignorer les fichiers de données locaux
*.db
*.sqlite
*.pkl
data/gazelle_vectors.pkl

# Ignorer les fichiers temporaires/de cache
__pycache__/
*.pyc

# Ignorer les secrets de configuration
.env
config/token.json
config/.env
config/env.txt
config/*credentials*.md
config/*secret*.md
*.token.json
token.json

# Cache de référence (peut être régénéré)
.reference_cache.json

# Ignorer les exports de données V4 (utilisés pour l'import initial)
*.csv
*.sql

# Ignorer les rapports des techniciens (données sensibles)
reports/
backups/

# Fichiers système macOS
.DS_Store
**/.DS_Store

# Fichiers temporaires et logs
*.pid
*.log
*.log.*

# Fichiers de credentials et variables d'environnement
data/credentials.json
frontend/.env.local
.env.local
.env.*.local

# Guides de migration avec secrets (webhooks Slack)
GUIDE_MIGRATION_*.md

# Fichiers de backup des secrets - NE JAMAIS pousser sur GitHub
SECRETS_PERMANENTS.md
docs/SECRETS_REFERENCE.md
