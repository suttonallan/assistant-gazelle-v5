# Règles Cursor pour Assistant Gazelle V5

## 📚 Consultation Obligatoire de la Référence

**AVANT TOUTE MODIFICATION OU ACTION, TOUJOURS:**

1. **Consulter `docs/REFERENCE_COMPLETE.md`** pour:
   - Mapping des techniciens (NE JAMAIS DEVINER)
   - Noms de colonnes valides (NE JAMAIS INVENTER)
   - Schéma des tables Supabase
   - Schéma SQL Server Gazelle (V4)
   - Mapping V4 → V5

2. **Utiliser `core/reference_manager.py`** dans les scripts:
   ```python
   from core.reference_manager import get_reference_manager
   ref = get_reference_manager()
   ref.ensure_reference_consulted("action_name")
   ```

3. **Mettre à jour la référence après chaque succès:**
   ```python
   ref.update_after_success("update_type", data)
   ```

## ⚠️ Règles Strictes

- **NE JAMAIS** deviner le mapping des techniciens
- **NE JAMAIS** inventer de noms de colonnes
- **TOUJOURS** vérifier dans la référence avant d'utiliser
- **TOUJOURS** mettre à jour la référence après un succès

## 📝 Workflow

1. Lire `docs/REFERENCE_COMPLETE.md`
2. Utiliser `ReferenceManager` pour valider
3. Effectuer l'action
4. Mettre à jour la référence si succès

