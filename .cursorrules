# Règles Cursor pour Assistant Gazelle V5

## 📖 Documentation à Lire au Démarrage

Quand tu démarres une nouvelle conversation dans ce projet, commence TOUJOURS par lire ces fichiers dans cet ordre:

1. **`docs/ETAT_SESSION_ACTUELLE.md`** - État du projet, travaux en cours, fonctionnalités complétées
2. **`INSTRUCTIONS_CURSOR_FINALISER_CLIENTS_CLIQUABLES.md`** - Tâche prioritaire en cours

## 🎯 Tâche Actuelle Prioritaire

Finaliser les clients cliquables dans le chat assistant. Tous les détails sont dans `INSTRUCTIONS_CURSOR_FINALISER_CLIENTS_CLIQUABLES.md`.

## 🚫 Règles Importantes

1. **Ne PAS over-engineer** - Solutions simples et directes uniquement
2. **Ne PAS créer de documentation** non demandée
3. **Ne PAS utiliser d'emojis** sauf si explicitement demandé
4. **Ne PAS modifier** les fichiers sans raison claire
5. **Toujours vérifier** `docs/ETAT_SESSION_ACTUELLE.md` avant de commencer

## 🏗️ Stack Technique

- **Backend:** FastAPI (Python) sur port 8000
- **Frontend:** React + Vite + Tailwind CSS
- **Base de données:** Supabase (PostgreSQL)
- **Auth:** PIN à 4 chiffres (simple)

## 📝 Conventions de Code

- **Python:** PEP 8, type hints optionnels
- **React:** Functional components, hooks
- **CSS:** Tailwind classes uniquement
- **Commits:** Messages en français, descriptifs

## ⚡ Commandes Rapides

```bash
# Backend
python3 -m uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload

# Frontend
cd frontend && npm run dev

# Déploiement
cd frontend && bash deploy-gh-pages.sh
```

## 💡 Rappels

- L'utilisateur travaille avec **GitHub Desktop** pour pousser (pas CLI)
- L'utilisateur préfère des **explications concises**
- Toujours **tester localement** avant de pousser
